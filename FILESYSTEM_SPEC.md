# Helicase SD Card Filesystem Specification

The authoritative filesystem, kit/instrument file, Scene storage, and
save/load target spec now lives at:

```text
knowledge_files/specification_reference/FILESYSTEM_SPEC.md
```

This root file remains as a compatibility pointer for older session notes and
open-editor references that still name `FILESYSTEM_SPEC.md` at the repository
root.
